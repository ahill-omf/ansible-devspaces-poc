---
- name: Update GitHub Pull Requests Package JSON
  hosts: localhost
  connection: local
  gather_facts: false
  no_log: false
  tasks:
    - name: Copy file with owner and permissions
      ansible.builtin.copy:
        src: /projects/ansible-devspaces-poc/files/github_pr_package.json
        dest: /checode/remote/extensions/github.vscode-pull-request-github-0.80.1-universal/package.json
        mode: '0664'
